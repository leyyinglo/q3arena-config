
// << ------------------------------ >>
// << KEY BINDS                      >>
// << ------------------------------ >>

unbindall

bind [ "echo FRAPS movie"
bind ] "echo FRAPS screenshot"

// << MOVEMENT/ACTION
bind w                      "+forward"
bind s                      "+back"
bind a                      "+moveleft"
bind d                      "+moveright"
bind mouse1                 "+attack"
bind space                  "+moveup"
bind c                      "+movedown"
bind t                      "+button3"
bind x                      "+button2"
bind shift                  "+zoom"

// << QUICK WEAPON
bind mouse2                 "weapon 7" //Railgun
bind e                      "weapon 6" //Lightning
bind q                      "weapon 5" //Rocket Launcher
bind r                      "weapon 8" //Plasma Gun
bind f                      "weapon 1" //Gauntlet "VANILLA KILLER" ;-))

// << WEAPON SCROLL
bind mwheeldown             "weapnext"
bind mwheelup               "weapprev"

// << MISC
bind tab                    "+scores"
bind `                      "toggleconsole"
bind ~                      "toggleconsole"
bind p                      "pause"

bind v                      ""
bind b                      ""
bind mouse3                 ""

bind KP_END                 ""

// << TEAM CHAT
bind 1                      "say_team [IV AT #L] FOLLOW ME #F"
bind 2                      "say_team [IV AT #L] [#h HEALTH] [#a ARMOR]"
bind 3                      "say_team [IV AT #L] AVAILABLE HERE [#I] [#K] [#R]"
bind 4                      "say_team [IV AT #L] I NEED [#M]"
bind 5                      "say_team [IV AT #L] HAVE [#P] [#U] [#W]"

// << STATS
bind 0                      ""
bind 9                      ""

// << FUNCTION KEYS
bind F1                     "vote yes; echo voted yes; say :-)"
bind F2                     "vote no;  echo voted no;  say ;-("
bind F3                     "ready;    echo ready;     say ;-"
bind F4                     ""

bind F5                     ""
bind F6                     "'
bind F7                     ""
bind F8                     ""

bind F9                     "autorecord"
bind F10                    "autoscreenshot"
bind F11                    ""
bind F12                    ""

// << AUDIO
bind INS                    "vstr volumeUp"  //Increase volume
bind DEL                    "vstr volumeDn"  //Decrease volume

// << QUIT
bind PAUSE                  "quit"

// EOF